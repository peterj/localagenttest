# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  localagenttest:
    image: localhost:5001/localagenttest:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["localagenttest", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=localagenttest
      - OTEL_SERVICE_NAME=localagenttest
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=gemini
      - MODEL_NAME=gemini-2.5-flash
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - type: bind
        source: ./localagenttest
        target: /config

